# Keep the daily refresh schedulers out of integration tests (no surprise
# external HTTP calls). Individual tests can re-enable one with e.g.
# @SpringBootTest(properties = "ricash.instrument-prices.refresh-enabled=true").
ricash.instrument-prices.refresh-enabled=false
ricash.exchange-rates.refresh-enabled=false
