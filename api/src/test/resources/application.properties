# Keep the daily instrument-price refresh scheduler out of integration tests
# (no surprise external HTTP calls). Individual tests can re-enable it with
# @SpringBootTest(properties = "ricash.instrument-prices.refresh-enabled=true").
ricash.instrument-prices.refresh-enabled=false
